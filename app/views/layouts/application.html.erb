<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Probesys" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    
    <%# securite %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# contenu supplementaire eventuel %>
    <%= yield :head %>

    <%# pour faire une application mobile (ignorer) %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# icones %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# tous les styles dans app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <%# Bootstrap %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%# Font Awesome %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

  </head>
  <body>
  
  <div class="container-fluid">
    <div class="row">
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Test Probesys</a>
        </div>
      </nav>
    </div>

    <div class="row">
      <div class="col-auto bg-light p-0 m-0 shadow" style="height: calc(100vh - 56px);">
        <div class="p-3">
          <h4 class="d-none d-sm-inline-block">Menu</h4>
          <ul class="nav flex-column">
            <li class="nav-item">
              <%= link_to root_path, class: "nav-link active p-0 mx-0 my-2" do %>
                <i class="fas fa-home me-2"></i> <span class="d-none d-sm-inline-block">Accueil</span>
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to teams_path, class: "nav-link active p-0 mx-0 my-2" do %>
                <i class="fas fa-users me-2"></i> <span class="d-none d-sm-inline-block">Equipes</span>
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to players_path, class: "nav-link active p-0 mx-0 my-2" do %>
                <i class="fas fa-user me-2"></i> <span class="d-none d-sm-inline-block">Joueurs</span>
              <% end %>
            </li>
          </ul>
        </div>
      </div>

      <div class="col p-2">
        <div class="row p-2 m-0 d-flex justify-content-center">
          <div class="col-12 col-md-10 col-lg-8 col-xl-6 card shadow p-3">
              <% if notice %>
                  <div class="alert alert-primary alert-dismissible fade show" role="alert">
                    <i class="fa-solid fa-circle-info"></i> <%= notice %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                  </div>
              <% end %>
              <%= yield %>
          </div>
        </div>  
      </div>
    </div>
  </div>

</body>
</html>
